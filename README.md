# What is this?
This launcher can be used to connect any RecRoom Build to a server, given you have a dll patch for that builds ssl. This also allow microservices to be used for the Server, as you can point this a reverse proxy and split the X-Forwarded-Host header into different services. This can help keep your code base clean.

Mraow :3
